on:
  push:
  pull_request:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: install packages
        run: |
          sudo apt-get -y update
          sudo apt-get -y upgrade
          sudo apt-get -y install emacs rust
      - run: make check
